# 桌面继续开发记录

## 用户要求与历史问题

- 用户曾确认最近任务侧滑与返回过渡无效；现已完成内屏与真实 CLOSED 外屏双向真机验证，范围与证据见下文。
- 统一桌面、搜索、文件夹、选择器、编辑菜单的圆角、配色及弹出/关闭动画。
- 应用页面应与图标混排现有 Android 小组件，不能仅在左侧组件栏使用。需要支持持久位置、移动、尺寸与分页，在内外屏切换时保持排布身份。
- 保持四应用短 Dock，搜索位于下方同一垂直中心线；玻璃外直接显示原色壁纸。

## 2026-09-15 当前进度

按用户新反馈再次调整控件：内外屏编辑/设置统一为 44dp 圆形局部玻璃线条图标（铅笔/调节），编辑时显示完成勾，保留无障碍名称和长按提示。外屏应用/小组件移至顶部同一行，移除整块厚重胶囊，只突出当前标签，释放应用区域高度。切换采用内容区 110ms 淡出与 220ms 淡入轻移，根布局、顶部操作入口和 Dock 不重建；关闭系统动画时立即切换，后台目录刷新等待切换结束。真实外屏双向切换测试检测到中间帧、最终透明度和位移归位、根/Dock 身份保持；翻页与目录刷新回归通过。截图 build/duo-controls.png 已检查。内屏共用图标控件已修改，设备当前合拢，未宣称本轮内屏实图已检查。

外屏补充验收：重新读取设备状态为真实 CLOSED、无 override，运行已安装版本的完整 HomeSmoke，通过文件夹图标、应用搜索清空/空结果、真实键盘 Insets、唯一选择、稳定桌面、现有组件 2×2/4×2 混排边界及无重叠。原布局由测试恢复；结束后确认 Duo 服务进入 crashed 列表，按现有授权列表重新挂载，随后 crashed 为空，force_fsg_nav_bar=1，返回 HOME。本轮分页样本 585 帧，P95 9.28 ms、0 帧超系统期限。该测试检查外屏静态和操作状态，仍未覆盖一次真实展开再合拢的连续画面。

设备真实合拢后，系统状态为 CLOSED，外屏逻辑尺寸 1168×1712。外屏截图 `build/duo-outer-fixed.png` 确认：四列四行应用及名称完整可见，四应用 Dock 与下方圆形搜索同轴，玻璃外直接显示原壁纸。窄屏顶部原先分开的“应用/小组件”入口已合并为统一的玻璃分段控件，应用页与组件页使用同一位置和选中样式。外屏 `paging_only` 通过双向斜滑、纵向路由、取消与后台目录延迟刷新，572 帧 P50 5.57 ms / P95 10.22 ms，1 帧超过系统期限。外屏 `navigation_smoke` 通过：小米原生最近任务左右侧滑均回到 Duo，各记录 27 个过渡中间帧；桌面边缘不触发返回。测试后按原有已授权服务列表恢复 Duo 辅助服务，默认 HOME 和兼容全面屏手势保持启用。下一项是用户展开设备时观察一次真实外屏到内屏再折回的连续交接。

独立配置 Activity 通路已验证：使用已安装抖音搜索工具 provider SearchWidgetWordWithToolsProvider2，系统单次授权后调用其配置 Activity，提供方自动返回成功，应用页创建实际 HostView、保存默认尺寸并正常移除。此次没有出现需要用户手动选择的配置表单，不能扩大为任意复杂表单已验证。第一次测试后发现旧临时 ID 153/155 残留：系统绑定已删除，但旧记录仍在保存列表。已增加启动时对本 Host 实际分配 ID 的核对、原子移除无效组件位置，移除回调读取最新保存列表；测试清理同时恢复 HomeWidgets.ids。修复后以 ID 156 重测通过，进程退出后独立读取真实偏好：组件列表仅 [70]，应用页无临时组件项。构建、Lint、事务/核对回归通过；已安装并同步 dist APK。此前“原布局已恢复”的测试结论只涵盖当时状态，本次补充了进程退出后的独立检查。

组件配置恢复弹窗已纳入 HomeWidgets.dismissPicker/close 清理，按 HOME 返回时跟随其他弹窗退出，不取消已保存的待添加事务。构建及 Lint 通过并已安装。dist/GlassProjection-0.5.0-preview.8.apk 已同步为完整 Gradle 构建的 versionCode 95，签名验证通过，附同名 .sha256；移除旧 APK 对应的不匹配 .idsig。后续不要再分发早期手工重打包版本。

系统取消组件添加已真机验证：临时日期组件 ID 154，实际系统授权弹窗点击“取消”，验证 ID 分配释放、pending ID/目标页清空、原组件列表/图标顺序/页码均不变。测试通过。已请求用户方便时合上手机停在 Duo 外屏，真实外屏检查等待该物理状态；不以模拟窗口尺寸代替实际折叠验证。

后台目录刷新改进已安装：应用列表读取完成时，若桌面正在接收触摸或分页仍处于拖动/惯性动画阶段，先保留旧视图，结束后再应用最新目录并保留目标页。HomeCatalogRefreshSmoke 真机验证滑动中提交目录、200ms 持续触摸期间根视图不变、松手后完成刷新且页码不回跳。斜滑/纵向/取消/侧栏稳定性回归通过，本次 578 帧 p95 12.77ms，1 帧超过系统期限。构建及 Lint 通过。配置变化引起的重新布局仍立即执行，以适应实际屏幕几何变化。

真实组件绑定流程已验证：HomeWidgetBindingSmoke 选择用户已有的“日期”提供方，临时分配 ID 153；通过实际系统绑定弹窗点击“创建”（不勾选始终允许），正常 onActivityResult 完成应用页放置，检查实际 AppWidgetHostView、默认尺寸持久化，调用正常移除释放绑定，最后恢复原组件列表和布局。测试通过。此提供方没有额外配置 Activity；带独立配置页的提供方与用户取消系统授权的路径仍需真机验证，不能将此结果扩大为所有提供方通过。

顶部视觉整理已安装：默认桌面隐藏内部 GLASS / HOME 标识，编辑与设置放入统一的局部玻璃胶囊，使用更深的控制背景改善浅色壁纸上的文字对比；常用文字按钮采用有圆角边界的原生按压涟漪。内屏截图 build/duo-header.png 已检查，四行图标、Dock、搜索完整可见，玻璃外仍显示原壁纸。构建及 Lint 通过。外屏视觉仍未实测。

组件缩放选项已按供应商 resizeMode、minResizeWidth/Height、maxResizeWidth/Height 和实际格子像素尺寸生成，保留当前尺寸以免内外屏变化破坏旧布局。选择后点“应用”才重排，取消不改布局；左栏高度调整也遵守供应商垂直缩放标记。不再固定只给 2×2/4×2/4×3/4×4。HomeWidgetResizeTest 覆盖固定轴、边距、上下限、无效声明和切屏保留当前尺寸；模型回归、完整构建及 Lint 通过。真实不同供应商组件缩放后的外观仍需检查。

翻页斜滑修复：HomePager 在系统触摸阈值后按整体方向锁定横向拖动，允许轻度斜滑；应用图标及名称不再参与子视图横向滚动竞争（真机查明 TextView 会返回可横向滚动，导致反向滑动被吞）。应用网格已按四行适配，移除外层冗余纵向 ScrollView。组件内部滚动仍交给其自身。paging_only 真机触摸回归通过：双向斜滑、应用名称区域、纵向手势、取消、边界、无障碍翻页、侧栏身份不变；579 帧 p50 6.64ms / p95 14.03ms，1 帧超过系统报告期限。这只代表本次内屏测试，不代表所有场景。安装后包变更异步刷新可能重建桌面，测试需在初始目录加载及窗口稳定后开始。

最近任务专项：用户明确授权持续将 Duo 设为默认 HOME，并启用兼容手势，用户需要时自行切回。真机已通过 HomeNavigationSmoke：触摸底部上滑停留进入原生最近任务，分别从左、右侧滑回 Duo，均记录 27 个进入动画中间绘制帧，最终焦点、透明度和缩放归位；桌面侧滑没有产生返回导航。覆盖的是当前内屏和本项目手势进入最近任务的路径，其他入口及外屏仍需验证。原生最近任务根节点为 `com.miui.home / android.view.View`，类名可能为空，因此采用成功 RECENTS 动作和活动窗口身份跟踪。手势取消不提交导航，桌面获得焦点后才播放进入动画。测试退出会杀掉目标进程，应重新绑定原有无障碍服务并恢复助手，不能留下隐藏导航栏却没有手势服务的状态。

已建立 HomeStyle 共用材质，面板采用 28dp 圆角、输入区域 18dp、圆形搜索 24dp。菜单与自定义面板共用 240ms 进入和 160ms 退出动画。供应商小组件自身内容样式由供应商保留。

应用页混排小组件首版已实现并安装：编辑模式“＋组件”添加到当前页；现有组件菜单可移入应用页；组件菜单可更换尺寸、移动页或移回左栏。网格排布支持可变占格并持久化，旧图标布局可读取。尚需继续验证外屏、绑定配置中断恢复、全部尺寸与交互动画。

完整构建、Lint、模型回归测试通过。真机使用已有绑定组件临时验证 2×2 / 4×2，与图标不重叠、边界不裁切、实际 AppWidgetHostView 保留，结束恢复原布局。真实绑定/配置的新建流程没有在本轮走完，不能据此宣称完成全部组件功能。截图发现组件硬边角，补充统一宿主裁剪圆角。组件 ID、目标页、完成和取消状态采用原子持久化；真机隔离数据验证完成、取消、过期结果、重建恢复和默认尺寸通过。新增与从左栏移入应用页的组件优先使用供应商 targetCellWidth/Height，否则按实际网格和像素默认尺寸计算。添加被中断后再次点添加可继续配置已有绑定组件，或重新选择。不同供应商配置 UI 与外屏仍待验证。

Windows JDK 回环连接故障的有效修复是为构建进程设置 `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=D:\localproject\GlassProjection\build\tmp`（先建立该目录）。该属性来自本机 JDK 的 UnixDomainSocketsUtil；仅调整 java.io.tmpdir 无效。应继续完整 Gradle 构建，不使用替换旧 APK DEX 的临时打包流程。
最近任务返回补充：识别到小米最近任务时预先记录返回状态，Duo 获得焦点后消费一次进入动画；打开其他应用清除记录。覆盖清理完成及点击空白退出，系统禁用动画时跳过。真实外屏 LEFT/RIGHT/BLANK/CLEAR 四条路径均采集到 27 个动画中间帧并归位。清理专项通过显式 clear_recents 参数启用，使用已读取的 1168×1712 原生清理按钮位置，等待系统清理完成；普通导航回归不执行清理。构建、Lint 通过，已安装同步 dist，测试后恢复已授权服务。
外屏清理返回真实运行修复（用户否定之前仪器测试结论后）：旧测试只采到视图内部帧，未证明最终合成画面。实际无 instrumentation 录屏复现清理后停在空最近任务；增加包含非重要节点的系统窗口检索，只在原生最近任务从有清理入口变为空内容时调用系统 HOME，窗口离开即停止观察。Duo 由自身离开/恢复生命周期请求返回动画，在 onEnterAnimationComplete 后下一帧播放，700ms 回调缺失兜底；等待期间准备初始状态，目录刷新延迟至动画结束，避免被遮挡提前播放和完整画面再次变淡。最终普通运行录屏 build/clear-final.mp4、逐帧图 build/clear-final-contact.png 已检查：原生清理、回到桌面、图标从半透明到清晰的可见过渡、最终稳定。构建与 Lint 通过，已安装并同步 dist。此记录替代先前仅凭 27 个内部帧宣称普通运行清理通过的结论；锁定任务残留等情形尚未扩展验证。
